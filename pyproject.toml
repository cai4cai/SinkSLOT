[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
# Distribution name for this repo (SinkSLOT). It packages two things under
# different attribution: the vendored flash_sinkhorn library (third-party
# prior work, MIT-licensed, kept under its own copyright -- see
# torch-ext/flash_sinkhorn/LICENSE) and this repo's own contribution
# (torch-ext/sinkslot/, gradient_flow/, configs/, the benchmark harness).
# `authors` below lists both, in that order; each covers only its own scope.
name = "sinkslot"
dynamic = ["version"]
description = "SinkSLOT: sparse Sinkhorn over a sliced-OT reference plan in PyTorch + Triton, built on the vendored FlashSinkhorn kernels below."
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
keywords = ["optimal-transport", "sinkhorn", "triton", "pytorch", "gpu"]
authors = [
  {name = "OT Triton Contributors"},
  {name = "CAI4CAI research group", email = "contact@cai4cai.uk"},
]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.9",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Scientific/Engineering",
  "Environment :: GPU :: NVIDIA CUDA",
]
dependencies = [
  "numpy",
  "torch>=2.5",
  "triton>=3.1",
]

[project.urls]
Homepage = "https://github.com/cai4cai/SinkSLOT"
Repository = "https://github.com/cai4cai/SinkSLOT"
Issues = "https://github.com/cai4cai/SinkSLOT/issues"
UpstreamFlashSinkhorn = "https://github.com/ot-triton-lab/flash-sinkhorn"

[project.optional-dependencies]
dev = [
  "pytest",
  "geomloss",
  "pykeops",
  "jax",
  "ott-jax",
  "matplotlib",
  "pillow",
  "pot",
  "rich",
  "textual",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["torch-ext"]

[tool.setuptools.dynamic]
version = {attr = "flash_sinkhorn.__version__"}

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["torch-ext/flash_sinkhorn/testing"]
